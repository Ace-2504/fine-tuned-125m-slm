# Copy to sft/.env and fill in. sft/.env is gitignored — never commit real keys.
GEMINI_API_KEY=your-gemini-api-key-here

# Optional free fallback teacher (Groq-hosted Llama 3.3 70B). Only needed if you
# switch the generator backend away from Gemini.
# GROQ_API_KEY=your-groq-api-key-here
